#!/usr/bin/env bash

function process_git() {
    git -C "./$1" status --branch --porcelain=v1
}

for x in $(ls); do
    echo --$x--
    if [[ -e $x/.git ]]; then
        process_git $x
        echo
    else
        echo "not a git repo"
    fi
done
